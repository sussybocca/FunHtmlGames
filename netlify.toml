[build]
  command = "dotnet publish FunHtmlGames.csproj -c Release -o publish /p:BaseIntermediateOutputPath=./tempobj/"
  publish = "publish/wwwroot"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
